# 2026-03-26 Gate-3 C13 最小验证用例（v1）

更新时间：2026-03-26  
范围：`C13`（AI 文本人性化改写）  
状态：待执行（仅定义口径，不直接放行到运行态）

## 1) 验证目标

- 验证“人性化改写”不引入事实漂移。
- 验证数字、引用、不确定性表达在改写后保持一致。
- 验证 `editor` 仅改表达，不改结论和因果关系。

## 2) 事实锁定规则（必须全部满足）

- 实体锁定：不得新增或替换人名、机构名、产品名、地名。
- 数字锁定：不得改动数字大小、单位、时间范围。
- 引用锁定：不得伪造引用来源，不得删除关键引用约束。
- 语义锁定：不得将“不确定”改写为“确定”。
- 因果锁定：不得新增“因果已证实”类表达。

## 3) 最小输出格式（必须全字段）

- 原句 / 原段：
- 改写后句段：
- 锁定类别（`数字/引用/不确定表述/实体/因果`）：
- 一致性检查结论（`通过/失败`）：
- 风险备注（若失败，说明失败点）：
- `fact_lock_notes`（说明“改了表达，未改事实”）：

## 4) 阻断规则（一票否决）

- 任一数字、实体、引用被篡改即失败。
- 将“可能/预计/待核实”改为“确定/已经证实”即失败。
- 未输出 `fact_lock_notes` 即失败。
- 越权到发布建议或发布执行即失败。

## 5) 回归绑定

- 角色回归：`E3`
- 端到端回归：`X4`
- 工程观察建议：新旧链路对照回归通过率 >= 98%

## 6) 最小样例（3 条）

### C13-POS-01（数字锁定样例）

- 输入要点：包含明确数字与时间范围（例如“30 天增长 12.5%”）。
- 期望：
  - 改写仅优化语气和可读性。
  - 数字、单位、时间范围完全不变。
- 判定：通过。

### C13-POS-02（引用锁定样例）

- 输入要点：包含“根据某报告/某官方公告”的引用句。
- 期望：
  - 保留引用来源与限定语。
  - 不新增不存在的引用主体。
- 判定：通过。

### C13-NEG-01（不确定表述漂移样例）

- 输入要点：原文包含“预计/可能/待核实”表达。
- 期望：
  - 改写后仍保留不确定语气。
  - 若被改成“已证实/确定”，则判失败。
- 判定：失败（若触发漂移）。

## 7) 执行记录（留空待回填）

- 执行日期：2026-03-26（Asia/Shanghai）
- 执行版本（v1）
- 执行人：Codex（with gstack expert review context）
- 结果汇总：通过
- 用例执行结果：
  - `C13-POS-01`：通过（runId=`2b7be5db-45ff-4bd5-85fb-0f7c6b895498`）
  - `C13-POS-02`：通过（runId=`8216bf08-df64-427e-84eb-fc878dbb03ab`）
  - `C13-NEG-01`：通过（runId=`68c37b15-b1fc-4f4a-b55f-faebc09800ef`）
- 失败项与修复动作：
  - 本轮无阻断失败
  - 观察项：可将 `fact_lock_notes` 字段模板化，便于后续自动评分
- 是否允许进入下一阶段（是/否）：是（C13 可进入 v2 受控试运行候选池）
