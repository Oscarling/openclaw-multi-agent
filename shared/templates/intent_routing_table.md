# Intent Routing Table (MVP)

## 路由入口

- 默认入口：`steward`

## 意图到角色

- 选题、竞品、评论洞察、素材线索 -> `hunter`
- 需求澄清、任务拆解、结果汇总 -> `steward`
- 正文成稿 -> 暂不进入（等待 `editor`）
- 发布动作 -> 暂不进入（等待 `publisher`）

## MVP 兜底规则

- 不清楚意图时，`steward` 先补问，不盲目分派
- 下游角色缺输入时，返回“缺失信息清单”
- 任何角色不得越权输出不属于本角色的最终产物
